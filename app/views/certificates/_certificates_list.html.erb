to jaja znoszący ssak który rusza na akcję
<% section.courses.each do |certificate|%>
  <%= render partial: 'certificates/edit', locals: { certificate: certificate } %>
<% end %>
<div class="buttons-container">
  <%= link_to user_cv_section_certificates_path(user_id: current_user.id, cv_id: @cv.id, section_id: section.id), method: :post, class: 'add-course', remote: true do %>
    <%= inline_svg_tag 'plus-icon.svg', class:"icon white round-button" %>
  <% end %>
</div>